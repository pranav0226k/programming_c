Program 13: Sum of digit of number.
#include <stdio.h>

void main(){
    int n, sum=0;
    printf("enter n:");
    scanf("%d",&n);
    while (n>0){
        sum=sum + (n%10);
        n=n/10;
    } printf("Sum of digit:%d",sum);
}
